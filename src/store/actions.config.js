export const config = process.env.NODE_ENV === 'development' ? {
  baseUrl: 'loaclhost',
  timeout: ''
} : {
  baseUrl: 'loaclhost',
  timeout: ''
}

export const addrs = {
  t0: {url: 'home', type: 'POST'}
}
